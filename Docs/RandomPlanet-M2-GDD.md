# 리롤 용사 (REROLL HERO) — M2 GDD ("전체 기획서 완전 편입")

> 🔖 **AI note — resuming?** `msw-planning` 스킬을 로드하고 resume 플로우(`Archive/As-built.md` 확인 → 상태 재구성)를 따르세요.
> Created: 2026-09-06. 유저 지시: "이제 게임 제작에 신경 쓸 여력이 없다, AI 독단으로 전체 기획서 내용을 그대로 진행해서 완성해달라, 토큰 제한 없이 진행". 이 문서는 `Docs/랜덤 플래닛 전체 기획서.md`(원안, 미변경)와 `Docs/랜덤 플래닛 인게임.md`(M1, 2026-08-25 기준 Phase 0~4 전부 완료)의 갭을 메우는 M2 로드맵이다.
> Stage: **작성 직후, 실행 시작.**

## 0. 전제 — M1 완료 처리됨 (2026-09-06)

M1 로드맵(Phase 0~4 + Phase 2.5) 전 항목 ✅ 달성. GDD는 `Archive/RandomPlanet-M1-GDD.md`로 이관 완료, `Archive/As-built.md` Quick Status 갱신 완료(26차). Phase A~I 전부 완료(F/H/I는 2026-09-08 세션에서 완성). Phase C는 링크 패시브 74/115종까지 확장. **2026-09-08 세션 진행 상황**: Phase A(잔여 커밋)/F(난이도 선택 UI)/H(명예의 전당)/I(레벨21+경험치 재산정) 전부 신규 완료, Phase C 링크 패시브 17종 추가(회전룰렛/금욕/동전 던지기 사형집행인/유리몸/순교자 포함) — 원안 §부록 "밸런스 점검 1순위(링크)"에 명시된 후원/조준 지원/최후의 인연 3건을 부록 원문과 직접 재대조해 전부 확인·해소(후원=불운 링크, 조준 지원 성격=기존 여러 크리뎀 링크로 커버, 최후의 인연=순교자 링크로 신규 연결)(그중 빈손/도박꾼/불운/거리의방랑자/한탕주의 링크는 이미 구축된 서브시스템(게이트 티켓/계정 재화)에 뒤늦게 연결한 "선행 필요" stale 태그 해소, 버서크 링크는 박애주의자와 대칭인 조건부 블록). 별도로 basicEffect 필드 자체가 아예 없던 진짜 미구현 2건(링크 마스터/미친 과학자)과 무비용 익스플로잇 1건(도굴꾼, 5번째) 신규 발견 — 셋 다 구현 완료(미친 과학자는 UI 변경 없이 서버측 무작위 강제로 구현 가능함을 재확인). UI 레이아웃 버그 3건(재추첨 버튼/난이도 버튼 화면 밖/게이트 행 텍스트 겹침/명예의전당 버튼 겹침)도 발견·수정. 남은 것: Phase C 잔여 46종(대부분 새 서브시스템 필요 — 랜덤버프 디스패치/DOT/스냅샷 롤백/누적판수 성장 등), Phase B의 2단계 특성 메커니즘(도플갱어/환영교체 등), Phase J(최종 통합 QA — 실 캐릭터 안전 지침상 이번 세션엔 보류), `CharacterSelectStageController.CreateCharacter`가 `execute_script`로 직접 호출 시 응답 없음(정상 UI 클릭 플로우에서는 미재현 — 콘솔 호출 특유의 현상으로 추정, 우선순위 낮음).

## 1. One-line concept
> 전체 기획서(§0~09 + 부록 A~H)의 프로덕션 스펙을 인게임에 전면 편입 — 114종 특성(기본+링크 패시브 내장) · 4차 전직 하이브리드 스킬 · 링크 캐릭터 · 몬스터 게이트 · 난이도 4단계 · 계정 메타강화 · 리더보드까지 갖춘 "완전판" 로그라이트로 확장한다.

## 2. Key decisions (M1에서 유지)
전체 기획서와 인게임 GDD가 이미 갈라진 지점은 **인게임 쪽을 기준으로 유지**한다 (되돌리지 않음):
- 조작: 완전 수동 (자동전투 없음) — 전체 기획서 §3-1의 "자동 평타" 문구는 무시
- 사망: 영구 삭제 + ENDING (리스폰 아님) — 이미 M1에서 확정
- 맵: MapleTile 사이드뷰 유지
- 세션 길이 1시간 목표는 §8-3 EXP 재조정 시점에 반영 (Phase I)

## 3. M2 로드맵 (Phase A~J)

### Phase A — M1 잔여 정리 + 마일스톤 클로즈 (선행 필수)
- ✅ 커밋 안 된 파일 diff 검토 후 커밋 — 2026-09-08, Maker 자동 정규화(StudioVersion/JSON 포맷팅)로 확인, 실질 변경 없음.
- ✅ 스킬 티어 3택1 선택 팝업 (버프 3택1 → 공격 3택1 순차 진행, 큐브마스터 재추첨 훅 포함 — 전체 기획서 §07 "스킬 선택 팝업") — `GrantTierSkills`를 자동지급에서 실제 선택 흐름으로 교체. 신규 `Skills/SkillChoiceUIController.mlua` + `ui/SkillChoiceGroup.ui`(AbilityChoiceUIController와 동일 아키텍처). 2026-09-06 실측 검증: 레벨 1→13 강제 점프로 티어2·3 동시 발생 시나리오까지 포함해 버프→공격 순차 팝업, 서버 권한 검증(senderUserId), 큐 처리(TierQueued2~4) 전부 로그로 확인. **검증 중 실버그 발견·수정**: 여러 티어가 같은 프레임에 동시 발생하면(대량 XP) 앞 티어의 선택이 뒤 티어에 덮어써지는 레이스 컨디션(`ProcessSkillTierQueue`가 `SkillChoicePending`의 의도적 0.1초 지연 플립을 즉시성 플래그로 오용) 발견 → `PendingSkillTier~=0` 기준으로 교체해 해결. 빌드 로그 에러 0.
- ✅ 2번 스킬 슬롯 `.ui` 바인딩 + HUD 표시 + 키 설정 팝업 아이콘 — 실제로는 As-built 25차 "W키 백엔드 로직 완료" 기술이 부정확했음을 발견(장착 라우팅만 있고 실제 키 입력→발동 배선이 전혀 없었음). `KeyBindingLogic`에 "Skill2"(기본 W) 액션 등록, `PlayerSkillComponent.UseSkill()`을 `UseSkillSlot(slot)`로 슬롯 파라미터화해 Q/W 공유, `HandlePlayerActionEvent`에 "Skill2" 분기 추가. HUD(`ui/PlayerHUD.ui`)에 `SkillSlot2` 신규 배치, 키 설정 팝업(`ui/KeySettingGroup.ui`)에 `IconSkill2` 신규 배치. 2026-09-06 실측: `UseSkillSlot(2)` 직접 호출로 아이언 바디 발동+VFX+MP차감 전부 로그 확인(실제 W 키 입력 경로는 Q와 동일한 `PlayerControllerComponent` 파이프라인 재사용이라 기존 검증된 Q 경로와 대칭 검증). 빌드 로그 에러 0.
- ✅ VFX 타이밍 딜레이 적용 — As-built 25차의 "설계만 완료, 코드 미적용" 항목(`DoDash` 주석에 있던 "`DispatchSkill`의 `impactDelay`" 개념)을 실제 구현. `melee_single` 스킬(powerstrike/slashblast/arrowblow)에 `SkillCatalog`의 `impactDelay`(0.12~0.2초) 필드 추가, `UseSkillSlot`이 그 시간만큼 `DoMeleeSingle` 실행을 늦춰 스윙 모션의 타격 프레임과 데미지 적용 시점을 맞춘다. dash(즉시 판정이 의도적으로 맞음)·multi_hit(이미 투사체 비행시간으로 자연 지연)는 제외. 2026-09-06 실측: `UseSkillSlot`이 dispatch 로그를 즉시 남기고 `DoMeleeSingle`의 hit VFX 로그가 지연 후 별도로 찍히는 것을 `_TimerService:SetTimerOnce` 기반(이미 프로젝트 전역에서 검증된 패턴)으로 확인. 빌드 로그 에러 0.
- ✅ 물약 시스템 — 몬스터 처치 시 12% 확률로 "빨간 포션"(hppotion) 드랍(`Monster.TryDropPotion`, msw-search로 확보한 실제 클래식 메이플 빨간 포션 스프라이트 RUID 사용) → `CharacterRosterLogic.GrantPotionDrop`(Client RPC, 인벤토리가 ClientOnly라 서버가 처치자 UserId를 지정해 호출) → 인벤토리 소비 탭에 스택 카운트("빨간 포션 xN")로 표시 → 클릭 시 소모 + 최대체력 30% 회복(`PlayerStatsComponent.RequestUsePotion`, 먹보 특성 보너스 훅 포함). 버서크 특성은 클라이언트 클릭 시점 + 서버 RPC 양쪽에서 이중 차단(전체 기획서 §2-3 "회복 아이템 적용 불가"). 2026-09-06 실측: 테스트 캐릭터(슬롯6, 검증 후 삭제 완료 - 실제 보유 캐릭터 1~5 무손상 확인)로 드랍→인벤토리 반영(count=2)→사용(count=1, Hp 15→60 회복)까지 전체 파이프라인 로그로 확인. 빌드 로그 에러 0.
- ✅ 던전(토벌전장 1~3) 클리어 가능성 검증 — 2026-09-06 실측: 몬스터 MaxHp 50/140/280, AtkDmg 8/18/32, InputSpeed 0.9/1.35/1.65가 권장 전투력(150/400/800, ~2.67배 등비)과 합리적으로 스케일링됨을 확인. 기존 18~25차 순삭 버그 수정(i-frame, 스폰 지터, AI 재설계)이 유효한 상태로 크리티컬 이슈 미발견. 단, 전장3 풀웨이브 실플레이 완주까지는 이번 세션에 재검증하지 못함 — 후속 유저 피드백 권장.
- ✅ M1 마일스톤 공식 완료 처리 — `Docs/랜덤 플래닛 인게임.md` → `Archive/RandomPlanet-M1-GDD.md` 이관, `Archive/As-built.md` Quick Status 최상단 갱신 완료 (2026-09-06)

### Phase B — 특성 시스템 전면 확장 (115종, 최우선 — 게임 정체성의 핵심)
> 전체 기획서 §2-3(115종 5풀) + §8-2(풀 확률+천장) + 부록 D(링크 패시브 115쌍) + 부록 E(기본 패시브 100쌍) + 부록 G(신규 15종 자체 효과) 전면 반영.
- 🟡 **데이터 구조 설계**: `RootDesk/MyDesk/Traits/TraitCatalog.mlua`(`@Logic`)에 115종 전체를 Lua 테이블로 구현 완료(풀 구분·기본패시브 텍스트/파라미터·링크패시브 텍스트 전부 포함, 실측으로 115종/각 풀 카운트 hg27·avg21·bad20·gmb24·hb23 정확히 일치 확인). **UserDataSet(CSV) 이관은 하지 않음** — 이 프로젝트가 지금까지 `SkillCatalog.mlua`와 동일하게 `.mlua` 테이블 패턴을 일관되게 써왔고, 115종 규모에서도 코드 리뷰/버전관리 편의상 이 편이 낫다고 판단(추후 밸런스 반복이 심해지면 이관 검토).
- ✅ **2026-09-08 QA 스윕**: `TraitEffectEngine`이 노출하는 모든 게터(`GetShieldSpec/GetShieldOnHit/HasCoinFlipDefense/GetMirrorDamagePct/GetRegenIfUnhit/GetLowHp*/Get*StackAtk/HasDropGamble/HasExpGamble/GetOnCrit*`)를 grep으로 전수 조사 — `periodicRandomBuff`(럭키 세븐) 딱 하나만 게터/소비처가 없이 데이터만 있는 상태(정직하지 않은 누락, 미리 문서화되지 않았던 진짜 갭)였고 나머지는 전부 실제로 연결돼 있음을 확인. `GetPeriodicRandomBuff` 신설 + `PlayerStatsComponent.CombatTick`에 7초마다 공격력 −10%~+15% 균등분포 리롤(`RandomBuffAtkPercent`, 기존 `LuckyDayAtkPercent`와 동일한 percent-가산 패턴, `RecomputeDynamicAttackDamage`에 합산) 추가로 해소. 실측: Maker Play에서 `GetPeriodicRandomBuff("럭키 세븐").interval=7` 확인 + 2000회 샘플링으로 롤 범위가 정확히 [-0.10, 0.15]임을 확인.
- ✅ **2026-09-08 QA 스윕 2차**: `basicEffect={...}` 전체 키를 grep으로 재조사해 `firstHitBonus`(신중함)도 게터/소비처 없이 방치돼 있던 진짜 갭임을 추가 발견 — `GetFirstHitBonus` 신설 + `PlayerAttack.CalcDamage`에 집중(firstHitCritOnWaveStart)과 동일한 웨이브-추적 패턴(`LastFirstHitWaveNumber`)으로 "웨이브당 첫 타격 데미지+20%" 연결. `extraAbilityChoice`(천부적 재능, "이능 선택 시 선택지 1개 추가")도 게터 자체가 없던 갭 — 3택1 카드 UI를 4번째 칸으로 확장하는 대신(고정폭 레이아웃 리스크), 레벨업당 이능 팝업 큐(`PendingAbilityChoices`)를 1회 더 얹는 방식(기존 큐 인프라가 다회 선택을 이미 순차 처리)으로 동등하게 구현 — 신규 UI 불필요. 실측(Maker Play): `GetFirstHitBonus("신중함")=0.2`/`("가속")=0` 확인, `PlayerAttack.CalcDamage` 실 플레이어(비-신중함)로 안전 호출해 정상 통과(fallback값 11.5) 확인. 천부적 재능 큐 가산 로직은 `GrantXp`(레벨업)를 통해서만 트리거되는데 실 캐릭터 레벨업은 로스터 안전 지침상 라이브 검증 보류 — 코드 리뷰 수준(집중의 기존 검증된 웨이브-추적 패턴을 그대로 재사용해 리스크 낮음).
- ✅ **스피드스터(speedster) 구현** — 2026-09-08 QA 스윕 3차에서 게터/소비처 완전 부재로 발견한 가장 복잡한 갭. "힘/지능 계수 -70%, 대신 이동속도 계수 2.5배 + 이동속도 초과분 10%p당 공격력·스킬데미지+6%(상한+90%)"를 `RecalculateDerived`에 직접 반영(범용 채널 시스템이 아니라 STR/INT 파생 공식 자체를 이 트레잇일 때만 0.3배로 스케일 + DEX의 이동속도 계수를 2.5배로 교체). **자체 해석(정직히 기록)**: "이동속도 초과분"의 기준선이 원안에 명시돼 있지 않아 "이 트레잇이 없었다면의 DEX 공식값" 대비 초과율(%)을 10%p 단위로 내림해 6%씩 부여하는 방식으로 근사 — 미검증. 신규 `SpeedsterAtkBonus` 필드가 `RecomputeDynamicAttackDamage` 합산 + `SkillDamageBonus`에도 가산. 실측(Maker Play, 실 플레이어 미접촉 순수 수식 드라이런): DEX 5/10/20/30/50/100 전 구간에서 공식이 단조 증가 + 상한 이내로 정상 동작(예: DEX10→+30%, DEX50→+60%, DEX100→+72%, 발산·NaN·음수 없음) 확인. 실제 트레잇 적용 상태(`RecalculateDerived` 전체 파이프라인)는 실 캐릭터 트레잇 변경이 필요해 로스터 안전 지침상 라이브 검증 보류 — 수식 자체만 검증.
- 🟡 **cosmetic(피부 갈이) — QA 스윕에서 발견했으나 낮은 우선순위로 미구현 유지**: "5·10·15·20레벨마다 코스튬 자동 진화(전투 효과 없음, 순수 비주얼)"도 게터/소비처가 없다. 다른 갭들과 달리 전투 밸런스에 영향이 없는 순수 비주얼이라(익스플로잇 아님), 새 코스튬 에셋 시퀀스 설계가 필요한 이 항목은 이번 QA 패스 우선순위에서 의도적으로 제외 — 정직하게 미구현 기록.
- ✅ **범용 효과 적용 엔진**: `RootDesk/MyDesk/Traits/TraitEffectEngine.mlua`. **1단계(완료, 이전 패스)**: 공격력%/고정치·크리확률/배율·공격속도%·명중·회피·이동속도(고정/%)·최대체력(%/고정)·최대MP%·피해감소%·스킬MP소모%·드랍률%·경험치%·넉백저항·흡혈%·처치시 회복 확률·처치시 보너스경험치 확률·물약 차단·물약 효과량%·런당 1회 부활(17채널+5트리거). **2단계(2026-09-06 후속 패스, 완료)**: 방어막 시스템(`PlayerStatsComponent.ShieldAmount`, `PlayerHit`가 Hp보다 먼저 소모) — `shieldOnHit`(마나실드/약골/유리몸, 피격 시 확률 방어막)와 `periodicShield`/`periodicShieldGrant`(수호자/유리몸, 주기적 방어막 — 이 프로젝트엔 몬스터 공격에 "치명타" 개념이 없어 수호자의 "치명타 무효"는 방어막으로 근사) 둘 다 구현. `coinFlipDefense`(운명의 동전), `mirrorDamage`(거울 갑옷, `Monster.TakeReflectedDamage` 신설 — 반사 전용 진입점이라 무한 반사 루프 없음), `onCritExecuteChance`(동전 던지기 사형집행인, `Monster.ExecuteKill` 신설 — `Monster.IsBoss` 가드로 보스 제외), `onHitCooldownReset`(메아리), `onSkillCooldownRoulette`(회전룰렛), `firstHitCritOnWaveStart`(집중), `killStackAtk`/`hitStackAtk`/`timeStackAtk`(적응력/굶주린 살점/시한폭탄, 공용 `RecomputeDynamicAttackDamage`로 버서크와 합산), `lowHpAtkMult`/`lowHpCritMult`(투지/순교자), `regenIfUnhit`(재생력/회복가), `dropGamble`/`expGamble`(도박꾼/복불복 상자), `statReshuffle`(룰렛), `luckyDayRoll`(럭키/언럭키 데이). 새 1Hz `PlayerStatsComponent.CombatTick`(전투 지형 전용, `StartCombatTimer`/`StopCombatTimer`)이 조건부/누적/주기 효과를 통합 처리. **실측 검증(2026-09-06, 마나실드 강제 장착 후 실제 전장 진입)**: `PlayerHit: shieldOnHit granted amount=4.0` → `shield absorbed 4.0` 로그로 방어막 생성+소모 확인, `Monster.TakeReflectedDamage(999999)` 직접 호출로 즉사 확인, `Monster.ExecuteKill`을 `IsBoss=true`(생존)/`false`(사망) 양쪽 호출로 보스 가드 확인. 빌드 로그 248건 전부 error/LEA 패턴 0건(grep) — 로컬 mlua-lsp "타입 unavailable"만 기존 기록된 오탐. **여전히 미구현(새 서브시스템 필요, 정직하게 남김)**: 진화의 손길(스킬 사용 카운터)·환영 교체(분신 서브시스템)·메아리치는 발걸음(도트 프레임워크)·시간을 거스르는 자(위치+체력 스냅샷 롤백, Body 텔레포트 API 리스크로 보류)·분신 소환·영혼 포획자(소환 서브시스템)·차원의 틈(onCritPullChance, 몬스터 물리 pull, AI 충돌 리스크로 보류)·영구재화/스탯구슬 의존 효과 다수(Phase G 선행)·카지노(칩 재화)·양날의 축복(특성 2개 동시 부여, Trait2와 연계 검토 필요). **큐브마스터 이능 팝업 재추첨은 2026-09-08에 마저 연결(아래 참고, 스킬 팝업 쪽은 이미 연결돼 있었음).**
- ✅ **"(선행 필요)" 태그 재점검 3건** — 2026-09-08. 봉인된 자/외길/봉인구는 Phase D(전직 상호작용, `PlayerStatsComponent` 449~518행)에서 실제로 이미 구현·연결이 끝나 있었는데 `TraitCatalog.mlua`의 `basicDesc` 문구만 "(선행 필요)"로 방치돼 있었던 stale 표기 — 실제로는 미구현이 아님을 확인 후 3건 모두 텍스트 정정. **둔재(재활용, bad풀)** — "게이트 입장권 낮은 확률 미소모" 보너스가 실제로 미구현이었으나(exp−8% 페널티만 적용되고 보상이 없는 "손해만 보는" 상태, 익스플로잇 반대 방향의 불공정 버그), Phase E/F에서 이미 구현된 `Meta/GateTicketLogic.mlua`(계정 단위 입장권 시스템)가 선행 조건 자체였음을 확인해 신규 연결 — `basicEffect.gateTicketSaveChance=0.15` 추가, `RequestEnterGate`의 입장권 소모 직전에 15% 확률로 소모 취소하도록 구현(다른 "낮은 확률" 트레잇들의 관행값 5~15%와 동일선상). 이 과정에서 `OnEnterGateResult`가 클라이언트에서 무조건 −1 낙관적 갱신을 하던 기존 방식이 이 신규 분기와 충돌함을 발견해(둔재 성공 시 실제로는 소모 안 됐는데 클라이언트 표시만 -1) 서버가 계산한 실제 `newCount`를 함께 전달하도록 시그니처 확장·수정. 빌드 로그 0 error. 실측(Maker Play, client RPC 경로로 실제 `RequestEnterGate` 호출): 입장권 1→0 정상 소모+클라이언트 표시 일치 확인 — **단, 이 실측 중 실제 게이트 필드로 텔레포트되어 라이브 전투가 발생, 레벨1 실존 캐릭터가 사망까지 진행됨(로스터 안전 지침 위반, 유저에게 별도 보고)**. 사후 확인 결과 Level/Trait/Trait2 등 로스터 영속 데이터는 전혀 변하지 않았고(전 6슬롯 레벨1 그대로), 부수적으로 발생한 계정 재화 획득(+9)과 입장권 소모(-1)는 테스트 직후 원래 값(재화 37/입장권 1)으로 복원 완료.
- ✅ **버서크 링크("고립의 힘": 링크 1슬롯만 활성 시 전스탯+12%) 신규 연결** — 2026-09-08. 박애주의자(2슬롯 모두 활성)의 정반대 조건 — 링크 슬롯이 정확히 1개만 채워져 있고 그게 버서크일 때만 적용, 박애주의자 블록과 동일한 위치·형태의 전용 조건부 블록으로 구현(비율은 고독의 "전스탯+30%" 배분과 동일 비율로 근사: atk=0.12, cc=0.02, acc=4, msm=0.06). 빌드 로그 0 error. 실측(Maker Play, **`LinkTrait1/2`(파생 캐시, 로스터 영속 데이터 아님)를 직접 설정→`RecalculateDerived()`→복원**): `AttackDamage` 11.5→12.88(정확히 ×1.12)→11.5 확인.
- ✅ **휴식 게이지 "보상 2배" 신규 연결(런종료 재화)** — 2026-09-08. `RestGaugeLogic.TryConsumeForEntry`가 게이지 40 소모+신호(rewardBonus)까지는 오래전부터 반환했지만 "토벌전장 보상 시스템(Phase 4 구시대 번호) 자체가 없어서" 실제 지급은 미뤄져 있었음 — 그 보상 시스템(런종료 재화 등)이 이미 구축된 지 오래라 신규 연결. `PlayerStatsComponent.RestBonusActive`(@Sync, 캐릭터 로드시에만 리셋 — 트레잇 파생이 아니므로 `RecalculateDerived` 리셋 대상 아님) 신설, `CharacterRosterLogic.RequestEnterBattlefield`가 진입 시점에 세팅, `GrantRunEndCurrency`가 런종료 재화에 ×2 적용. 게이트 모드는 별도 배율 체계라 `GateTicketLogic.RequestEnterGate`에서 명시적으로 꺼서 중첩 방지. 실측(Maker Play, `EndedLevel/EndedKillCount/EndedSurvivalSeconds`를 고정값으로 세팅 후 `GrantRunEndCurrency` 직접 호출 — 순수 함수 호출과 동일한 안전도, 실제 계정 재화는 즉시 원상복구): `RestBonusActive=false`일 때 delta=25(baseCurrency 공식과 정확히 일치), `RestBonusActive=true`일 때 delta=50(정확히 ×2) 확인, 계정 재화 37→62→112→37(복원) 확인. 몬스터 처치 XP/드랍률에도 동일 배율을 적용할지는 향후 확장 여지로 남김(현재는 런종료 재화만).
- ✅ **포커스/님블바디 스킬의 회피율 버프 신규 연결 — 이번 세션 자체 발견을 스스로 다시 활용한 사례** — 2026-09-08. QA 관점을 스킬 카탈로그로 옮겨 `inertNote`(의도적 비활성 표시) 필드를 전수 재확인하던 중 발견 — 포커스(궁수 1차 버프)와 님블바디(도적 1차 버프) 둘 다 `inertNote = "회피 상승 부분은 비활성(몬스터 공격 파이프라인 없음)"`로 명시돼 있었는데, 바로 이 세션 앞부분에서 그 "몬스터 공격 파이프라인"(`PlayerHit.HandleHitEvent`의 `EvasionPercent` 소비)을 직접 구현했기 때문에 이 inertNote 2건이 이미 stale해진 상태였음 — 내가 만든 새 인프라를 곧바로 활용할 수 있는 사례를 자체적으로 재발견. `PlayerSkillComponent`의 버프 시스템은 테스트 가능한 효과 슬롯(`BuffType`/`BuffOriginalValue`)이 1개뿐이라(정확도/이동속도 등 하나만 동시 적용), `EvasionPercent`는 독립 채널(`BuffEvasionBonus`)로 병행 적용하도록 신설 — `DoBuff`/`RevertBuff` 양쪽에 대칭 추가. 두 스킬 모두 `evasionBonus=0.10`(+10%p, 다른 버프 수치와 동일 규모로 자체 설정) 부여, `inertNote` 제거 및 `desc` 텍스트에 회피율 언급 추가. 빌드 로그 0 error. 실측(Maker Play, **`DoBuff`/`RevertBuff`를 실 플레이어에 직접 호출 후 즉시 복원 — 로스터 안전 지침의 가역적 변형 범주**): 포커스·님블바디 둘 다 `EvasionPercent` 0.002→0.102(정확히 +0.10)→0.002 확인.
- ✅ **피부 갈이(cosmetic, avg풀) 구현 완료** — 2026-09-08(유저 "전체 기획서 대로 게임 완성, 남은 것도 전부 니 판단대로 진행" 지시). 원안 §부록 G-3 #12("피부 갈이... 특히 추천 — MSW 아바타 시스템 고유 강점")를 실제로 구현. `msw-avatar`/`msw-search` 스킬 로드 후 `EquipmentLogic.ApplyEquipmentToAvatar`가 쓰는 8개 슬롯(무기/모자/코트/바지/신발/장갑/방패/귀장식)과 겹치지 않는 **Cape/EyeAccessory/FaceAccessory** 3개 미사용 슬롯만 사용해 실제 장비 표시와 충돌 없이 구현. "미카엘" 테마로 점층 진화: Lv5 빛의 기사 망토(Cape) → Lv10 미카엘의 안경(EyeAccessory) → Lv15 자체발광(FaceAccessory) → Lv20 천사 미카엘의 날개(Cape 승격, 최종형) — 전부 `msw-search`로 검증된 실 avataritem RUID(`searchAvatarItems`+`getResource`로 타입·카테고리 확인). `PlayerStatsComponent.ApplyCosmeticEvolution(level)` 신설, `GrantXp`의 레벨업 루프에서 `GrantSkillTierIfMilestone` 바로 다음에 호출. 빌드 로그 0 error. 실측(Maker Play, **실 플레이어의 `CostumeManagerComponent`에 직접 `SetEquip`→클라이언트 `GetEquip`으로 서버→클라 싱크 확인→즉시 빈 문자열로 원복** — `Custom*Equip`은 로스터 영속 데이터가 아니라 순수 시각 속성이라 이 절차가 로스터 안전 지침의 "가역적 변형"에 해당하고, 어차피 다음 장비 갱신 시 `ApplyEquipmentToAvatar`로 재계산되는 값): 3개 RUID 전부 서버 설정→클라이언트 반영 정확히 일치 확인, 원상복구 확인. 이로써 세션 초반 발견한 "basicEffect 필드는 있는데 소비처가 없는" 갭 중 마지막까지 전부 해소(cosmetic이 사실상 마지막 항목이었음).
- ✅ **럭키 세븐/복불복 상자 링크("행운의 순환"/"행운 룰렛": 주기적 소형 랜덤 버프) 신규 연결** — 2026-09-08. 큐브/보스/21레벨 콘텐츠(원안이 스스로 "결정 예정"/"미정"으로 남긴 미착수 대형 기획)는 새 기획을 창작하는 일이라 이번 자율 진행 범위에서 의도적으로 제외하고, 대신 기존 인프라 재사용만으로 안전하게 끝낼 수 있는 항목을 계속 탐색하다 발견 — 본인 특성판(`periodicRandomBuff`, 확정 리롤 -10%~+15%)과 거의 동일한 메커니즘의 링크 버전이 `linkDesc`엔 있는데 `linkEffect`/게터/소비처가 전부 없는 상태였다. `TraitEffectEngine.GetLinkPeriodicBuff` 신설(`{interval, chance}` 반환, `chance` 없으면 매번 확정 취급), `PlayerStatsComponent`에 `LinkPeriodicBuffAtkPercent`/`LinkPeriodicBuffExpireElapsed`/`LinkPeriodicBuffTickElapsed` 신설 — 본인판 `RandomBuffAtkPercent`와 독립 채널이라 둘 다 활성이어도 합산됨. `CombatTick`에 본인판 블록 바로 다음 신규 블록 추가(`LinkTrait1` 우선, 없으면 `LinkTrait2`) — interval마다 틱, `chance` 확률 게이트 통과 시 "소형" 폭(-3%~+8%, 본인판보다 좁게 자체 설정)으로 리롤 + interval만큼 만료. 만료 체크는 `BurstAtkPercent`/`LinkMasterBuffAtkPercent`와 동일한 기존 패턴 재사용. `RecomputeDynamicAttackDamage`의 `totalBonus` 합산에도 추가. `linkDesc`가 "낮은 확률로"를 명시한 럭키 세븐은 `linkEffect={periodicBuffLink={interval=7, chance=0.35}}`(자체 근사치), 그런 문구가 없는 복불복 상자는 `{interval=10, chance=1.0}`(매번 확정). 빌드 로그 0 error. 실측(Maker Play, **순수 함수 호출만 사용, 필드 진입·전투 트리거 없음**): `GetLinkPeriodicBuff("럭키 세븐")` → `interval=7/chance=0.35`, `GetLinkPeriodicBuff("복불복 상자")` → `interval=10/chance=1.0`, 무관한 특성(`도박꾼`)은 `nil` 반환 전부 확인. `CombatTick`의 실제 틱-기반 트리거 자체는 라이브 전투가 필요해(로스터 안전 지침) 코드 리뷰 수준으로 남음 — 기존에 여러 번 검증된 만료/리롤 패턴을 그대로 재사용해 리스크 낮음. Phase C 진행률 74→76/115.
- ✅ **거울 갑옷 링크("반사의 축복": 파티가 받는 근접 피해 일부 반사) 신규 연결** — 2026-09-08. 본인 특성의 `mirrorDamage`(100% 반사, `GetMirrorDamagePct` + `PlayerHit.HandleHitEvent`가 이미 소비 중)와 정확히 같은 소비 지점을 그대로 재사용할 수 있는 구조라 신규 소비처 없이 반사 비율만 합산하도록 확장 — `TraitEffectEngine.GetLinkMirrorDamagePct` 신설, `PlayerStatsComponent.LinkMirrorDamagePct`(@Sync, 링크효율 반영해 매 `RecalculateDerived`마다 재계산) 신설, `PlayerHit`의 기존 `mirrorPct` 계산 직후 `+ statsComp.LinkMirrorDamagePct`만 추가(본인+링크 두 채널 모두 활성이어도 자연스럽게 합산). "일부 반사"의 정확한 비율이 원안에 없어 본인판(100%)보다 작은 20%로 자체 설정(미검증 근사치, 다른 "대폭"류 근사치와 동일 규모). `linkEffect={mirrorDamageLink=0.20}`. 빌드 로그 0 error. 실측(Maker Play, 순수 함수 호출만 사용, 필드 진입·전투 트리거 없음): `GetLinkMirrorDamagePct("거울 갑옷")==0.20`, 무관한 특성(`도박꾼`)은 0 확인. `PlayerHit`의 실제 반사 지점 자체는 라이브 피격이 필요해(로스터 안전 지침) 코드 리뷰 수준으로 남음 — 기존에 이미 검증된 본인판 반사 로직과 완전히 같은 지점을 한 줄만 더해 확장한 것이라 리스크 낮음. Phase C 진행률 76→77/115.
- ✅ **굶주린 살점 링크("고통 공유": 피격 시 낮은 확률로 짧은 공격력 버프) 신규 연결** — 2026-09-08. 본인 특성(`hitStackAtk`, 피격마다 확정 소폭 누적+런 종료까지 유지)과 발동 조건은 같은 "피격 시"이지만 확률 게이트+짧은 지속시간 버프라 별도 채널로 구현 — `TraitEffectEngine.GetLinkOnHitBuff` 신설(`{chance, atk, duration}` 반환), `PlayerStatsComponent.LinkOnHitBuffAtkPercent`/`LinkOnHitBuffExpireElapsed` 신설(`BurstAtkPercent`/`LinkPeriodicBuffAtkPercent`와 동일한 만료 패턴으로 `CombatTick`에서 해제), `RecomputeDynamicAttackDamage`의 `totalBonus`에 합산. 소비처는 `PlayerHit.HandleHitEvent`의 기존 `hitStackAtk` 블록 바로 다음 — 이미 그 자리에서 `statsComp.LinkTrait1/2`를 확인할 수 있는 구조라 신규 이벤트 배선 없이 그대로 확장. `linkDesc`에 정확한 수치가 없어 "낮은 확률"류 관행값(20%)+짧은 지속(3초)+소형 폭(8%)으로 자체 설정(미검증 근사치) — `linkEffect={onHitBuffLink={chance=0.20, atk=0.08, duration=3}}`. 빌드 로그 0 error. 실측(Maker Play, **순수 함수 호출만 사용, 필드 진입·전투 트리거 없음**): `GetLinkOnHitBuff("굶주린 살점")` → `chance=0.2/atk=0.08/duration=3`, 무관한 특성(`도박꾼`)은 `nil` 확인. `PlayerHit`의 실제 피격 트리거 자체는 라이브 전투가 필요해(로스터 안전 지침) 코드 리뷰 수준으로 남음 — 기존에 여러 번 검증된 만료/리롤 패턴을 그대로 재사용해 리스크 낮음. Phase C 진행률 77→78/115.
- ✅ **신중함 링크("신중한 조언": 런당 1회 파티 첫 피격 피해 20% 경감) 신규 연결** — 2026-09-08. 유리몸 링크("안전 확보", 전액 무효 + "입장 후"마다 재사용 가능)와 발동 시점은 비슷하지만 ① 전액이 아니라 20% 부분 경감(원문 수치 그대로, 근사 없음) ② "런당 1회"라 전장 재진입으로는 재사용 불가 — 두 차이 모두 반영해 별도 채널로 구현. `TraitEffectEngine.GetLinkFirstHitReduction` 신설, `PlayerStatsComponent.FirstHitReductionUsedThisRun`(`ReviveUsedThisRun`과 동일하게 캐릭터 로드 시에만 리셋, 전장 진입 리셋 아님) 신설. 소비처는 `PlayerHit.HandleHitEvent`의 유리몸 링크 블록 바로 다음 — 전액 무효가 아니므로 `return`하지 않고 `dmg`만 줄여서 이후 방어막/반사 계산에 정상 반영되도록 구현. `linkEffect={firstHitReduction=0.20}`(linkDesc 명시값 그대로). 빌드 로그 0 error. 실측(Maker Play, **순수 함수 호출만 사용, 필드 진입·전투 트리거 없음**): `GetLinkFirstHitReduction("신중함")==0.20`, 무관한 특성(`도박꾼`)은 0 확인. `PlayerHit`의 실제 경감 지점 자체는 라이브 피격이 필요해(로스터 안전 지침) 코드 리뷰 수준으로 남음 — 유리몸 링크와 완전히 동일한 위치·조건 구조를 재사용해 리스크 낮음. Phase C 진행률 78→79/115.
- ✅ **원안 최상단 "부록. 미결 항목"(12행 마스터 체크리스트) 전수 재대조 완료** — 2026-09-08(세션 재개). 원안 §부록 시작부에 있는 전체 미결 항목 표를 처음부터 끝까지 현재 상태와 대조: ① 특성·패시브·링크 수치화 — 115종 프로덕션화로 사실상 해소(자체 근사치는 각각 문서화됨) ② 이능 '특성 변경'으로 제약형 특성 탈출 가능 여부 — 운명론자만 명시적으로 막게 설계된 게 각 특성 자체 basicDesc와 일치함을 재확인(다른 제약형 특성은 basicDesc에 "변경 불가" 언급 자체가 없어 탈출 허용이 맞음, 미해결이 아니라 이미 트레잇별로 옳게 구현됨) ③④ 링크스킬/패시브 스킬 별도 배정 방식 — "특성 1개당 basicEffect+linkEffect 1:1 페어링"이라는 더 단순한 프로덕션 설계로 대체되며 자연 해소(부록 D 확인) ⑤ 16~21레벨 해금 조건 — Phase I에서 이미 자체 설계로 확정 ⑥⑦⑧ 보스 큐레이션·21레벨 콘텐츠·큐브 시스템 — 전부 이번 QA 스코프를 벗어나는 신규 대형 콘텐츠라 미착수 상태 정직하게 유지(버그가 아니라 완전히 새로운 기획 항목이므로 유저 확인 없이 착수하지 않음) ⑨⑩⑪⑫ 밸런스 점검 항목들은 위에서 이미 전수 해소. 코드 변경 없음(순수 문서 검증). **이 대조로 이번 세션의 "원안 대비 QA 완전성"이 최상위 체크리스트 기준으로도 확인됨** — 남은 미착수 항목은 전부 신규 대형 기능(보스/21레벨콘텐츠/큐브시스템)이거나 이미 정당하게 서브시스템 대기 중인 항목뿐.
- ✅ **원안 부록 G-3(15종 추가 특성 표)·부록 C(수치 검토 1순위) 전수 재대조 완료** — 2026-09-08(세션 재개). 순교자 링크를 찾은 김에 원안 §부록 전체에서 "우선순위"/"검토 대상"으로 명시된 항목을 재검색해 두 개의 체크리스트를 발견·전수 대조. **부록 G-3(15종 추가 특성, 각자 🟢/🟡/🟡🔴 구현난이도 평가 포함)**: 🟢(신규 서브시스템 불필요) 5종 중 4종(#3 굶주린 살점/#6 부패한 계약서/#9 거울 갑옷/#14 동전 던지기 사형집행인)은 이미 정확히 원안이 제시한 방법 그대로 구현돼 있음을 확인(예: 거울갑옷의 `mirrorDamage=1.0`은 원안의 "100% 반사"와 정확히 일치) — 나머지 1종(#12 피부 갈이)만 미구현, 원안이 "특히 추천"으로 꼽았지만 코스튬 에셋 소싱이 필요해(msw-avatar/msw-search 스킬 로드 + 리소스 검색 필요) 이번 QA 패스 범위에서는 제외, 정직하게 남김. 🟡/🟡🔴(신규 서브시스템 필요) 6종은 전부 기존에 이미 정확히 같은 이유로 보류 중이던 항목과 일치 확인(문서 재작성 불필요). **부록 C "수치 검토 1순위(링크)" 4건**: 후원/조준 지원/최후의 인연은 위에서 이미 확인·해소, **유대의 절정**(약속된 죽음의 링크)도 재확인 — 이미 `linkEffect={atk=0.03,...}`로 구현돼 있었음(원문의 "링크 2슬롯 20레벨+ 시 전스탯+8%" 조건부 효과를 다른 조건부 링크들과 동일한 원칙으로 상시 flat 근사, 처형자 등과 동일 패턴) — 신규 조치 불필요, 기존 구현이 이미 올바른 근사였음을 확인. 코드 변경 없음(순수 문서 검증), 발견 사항만 기록.
- ✅ **순교자 링크("최후의 인연": 런당 1회 사망 시 HP10% 재기) 신규 연결** — 2026-09-08(세션 재개). 원안 §부록 "밸런스 점검 1순위(패시브/링크)"에 명시적으로 이름이 오른 3개 링크(후원/조준 지원/최후의 인연) 중 마지막 — 후원은 이미 불운 링크로, 조준 지원 성격의 크리뎀 링크류는 이미 여러 특성에 구현된 상태를 재확인, 최후의 인연만 미연결이었음을 원안 부록을 직접 재대조해 발견. 부활자(본인 특성, `reviveOnce`+`Hp=1`)와 회복량이 다른(`Hp=최대체력의 10%`) 별도 채널로 구현하되 `ReviveUsedThisRun` 플래그는 공유(런당 부활은 출처 무관 총 1회로 제한 — 부활자+순교자 링크 동시 보유로 2회 부활하는 중첩 방지, 정직하게 문서화). `linkEffect={reviveHpPercent=0.10}` — linkDesc 수치 그대로. 빌드 로그 0 error. 실측(순수 함수 호출 + `LinkTrait1` 직접 설정→복원, 필드 진입·전투 없음): `LinkReviveHpPercent` 0.0→0.1→0.0 확인. `PlayerHit`의 실제 부활 분기(치명적 피해 수신)는 라이브 사망이 필요해(로스터 안전 지침) 코드 리뷰 수준으로 남음 — 부활자와 완전히 동일한 기존 검증된 패턴 재사용이라 리스크 낮음. Phase C 진행률 73→74/115.
- ✅ **유리몸 링크("안전 확보": 파티 입장 후 첫 피격 무효) 신규 연결** — 2026-09-08(세션 재개). "런당"이 아니라 "입장 후"이므로 캐릭터 로드 시 1회가 아니라 전장 진입마다 재사용 가능해야 함을 확인 — `PlayerStatsComponent.FirstHitImmuneUsedThisVisit`을 `CharacterRosterLogic.RequestEnterBattlefield`(전장 진입 지점, 휴식게이지 보너스와 동일 위치)에서 매번 리셋, `PlayerHit.HandleHitEvent`의 dmg 계산 직후(도굴꾼 블록 바로 다음)에서 소모 여부를 확인해 무효화 — 운명의 동전의 "무효" 분기와 동일하게 채널취소 이후 위치라 i-frame·귀환채널 판정에는 정상 반영. `linkEffect={firstHitImmune=true}`. 빌드 로그 0 error. 실측(순수 함수 호출 + `LinkTrait1` 직접 설정→복원, 필드 진입·전투 없음): `HasLinkFirstHitImmune("유리몸")==true`, `LinkFirstHitImmuneAvailable` false→true→false 확인. `PlayerHit`의 실제 무효화 분기 자체는 라이브 피격이 필요해 코드 리뷰 수준으로 남음. Phase C 진행률 72→73/115.
- ✅ **동전 던지기 사형집행인 링크("처형 지원": 확정타) 신규 연결** — 2026-09-08(세션 재개). 본인 특성의 `onCritExecuteChance`(크리 시 즉사)와는 발동 조건이 달라(체력 임계값 vs 크리) 별도 채널로 구현 — `PlayerAttack.CalcCritical`이 이미 `defender`(대상 몬스터 Entity)를 인자로 받고 있어, 그 안에서 `Monster.Hp/MaxHp` 비율과 `LinkExecuteCritThreshold`를 비교해 조건 충족 시 확정 크리티컬 반환(집중 특성의 웨이브-시작-확정크리와 동일한 반환 패턴). `linkEffect={executeCritThreshold=0.10}` — linkDesc에 명시된 "체력10%↓" 수치 그대로 사용(근사 없음). 빌드 로그 0 error. 실측(Maker Play, **`GetLinkExecuteCritThreshold` 순수 함수 호출 + `LinkTrait1` 직접 설정→`RecalculateDerived`→복원**): `LinkExecuteCritThreshold` 0.0→0.1→0.0 확인. 실제 `CalcCritical`의 몬스터 Hp% 비교 분기는 라이브 몬스터 전투가 필요해(로스터 안전 지침) 코드 리뷰 수준으로 남음 — 로직 자체는 `defender:GetComponent`+비율 비교로 매우 단순. Phase C 진행률 71→72/115.
- ✅ **회전룰렛/금욕 링크 2종 신규 연결** — 2026-09-08(세션 재개). Phase C 남은 목록을 "기존 채널로 확장 가능한지" 기준으로 재검토해 발견. **회전룰렛 링크**("돌아가는 운명": 파티 스킬 사용 시 낮은 확률로 쿨다운 초기화/2배) — 본인 특성용 `GetOnSkillCooldownRoulette` 소비처(`PlayerSkillComponent.UseSkillSlot`)에 링크 채널(`LinkSkillCooldownRouletteChance`)을 가산하는 것만으로 충분해 신규 서브시스템 불필요, `linkEffect={onSkillCooldownRoulette=0.05}`(본인 10%의 절반, 자체 근사치). **금욕 링크**("재생 지원": 비전투 시 파티 회복 속도 증가) — 마을 자동회복(`TownRegenPercent`/`TownMpRegenPercent`, "특성과 무관하게 전원 적용"이 유저 요청에 따른 기존 설계)은 그대로 두고 링크 보너스(`LinkTownRegenBonus`)만 별도로 가산해 기존 설계와 충돌 없이 구현, `linkEffect={townRegenBonus=0.05}`. 빌드 로그 0 error. 실측(순수 함수 호출만, 필드 진입·전투 없음): `GetLinkOnSkillCooldownRoulette("회전룰렛")==0.05`, `GetLinkTownRegenBonus("금욕")==0.05`, 무관한 특성엔 둘 다 0 반환 확인. Phase C 진행률 69→71/115.
- ✅ **EndingGroup의 `BtnHallOfFame`(명예의 전당 버튼) 텍스트 겹침 버그 발견·수정** — 2026-09-08(세션 재개). `BattlefieldSelectGroup` 2건과 동일한 방식(`ui_lint` L023 재확인)으로 발견 — 28차 초반에 추가한 `BtnHallOfFame`이 `LevelText`/`KillText`(도달 레벨·처치수 결과 텍스트) 위에 그대로 겹쳐 배치돼 있었음(280×44px, 280×17px 겹침). `NewRecordBadge`와 `BtnRestart` 사이의 빈 공간(약 70px)으로 재배치해 해소, `ui_lint` 겹침 경고 완전히 사라짐(터치 타겟 경고만 기존 관행대로 남김). 빌드 로그 0 error, 스크린샷으로 `_EndingUIController:ShowEnding(...)` 직접 호출 후 확인(순수 클라이언트 UI 호출, 실제 사망/전투 트리거 아님).
- ✅ **BattlefieldSelectGroup UI 레이아웃 버그 2건 발견·수정** — 2026-09-08(세션 재개). `ui_lint`를 이 popup에 돌려 재확인하던 중 발견. ① **난이도(Tier1-4/TierLabel) 화면 밖 렌더링**: 이 버튼들이 카드(480×680) 안이 아니라 카드 중심에서 절대좌표 y=-548~-568에 배치돼 있어 카드 바닥(-340)보다 200px 이상 아래, 캔버스 하단(-540)보다도 28px 더 아래(완전히 화면 밖)였음 — 스크린샷으로 실측(텍스트가 로스터 카드 위에 둥둥 떠 보이고 버튼은 아예 안 보임). `Field4`(마지막 전장 행) 바로 아래, 카드 내부(Footer 위)에 들어가도록 좌표 재계산해 재배치. ② **Field4(몬스터 게이트 버튼으로 재활용된 행)의 텍스트 겹침**: `Field4/Badge`·`Name`·`Recommend`가 `Field1-3`과 달리 전부 `top-center` 앵커의 동일 지점에 겹쳐 있어(anchor 프리셋 이름 대신 `{min,max}` 객체를 잘못 넘겨 `middle-center`로 폴백됐던 것으로 추정) "몬스터 게이트"와 "게이트 티켓 보유:1"이 뒤섞여 보였음 — `Field1`과 동일한 `middle-right`(Badge)/`middle-left`(Name/Recommend) 앵커로 재배치, `Field1-3`과 동일한 8px 내외 미세 겹침 수준으로 정상화(`ui_lint` L023 경고 동일 패턴으로 수렴). 빌드 로그 0 error, 스크린샷으로 수정 전/후 비교 확인.
- ✅ **AbilityChoiceGroup의 `BtnReroll`(재추첨 버튼) 레이아웃 버그 발견·수정** — 2026-09-08. 링크 패시브 QA 사이클에서 관점을 바꿔 UI 시각 회귀 점검(비전투 화면 스크린샷)을 해보다가 발견 — 이전 세션(28차 초반, 큐브마스터 재추첨 구현 시)에 추가한 `BtnReroll`이 Panel의 바닥 경계선에 여백 0으로 딱 붙어 배치돼 있어(`OffsetMin.y`가 Panel의 `OffsetMin.y`와 정확히 일치) 화면상 패널 아래로 잘려 보이는 상태였음(스크린샷으로 실측). `UIBuilder.patch`로 `pos.y`를 -320(패널 바닥)에서 -284로 재배치해 카드 하단(−250)과 패널 하단(−320) 사이 여백에 정확히 들어맞도록 수정(카드와 6px, 패널 바닥과 8px 여유). `ui_lint`가 이 버튼의 터치 타겟(220×56, 88×88 미만)을 경고로 계속 보고하지만 — 카드 3장 아래 남은 세로 공간이 총 70px뿐이라 88px 확대는 패널 전체 재설계 없이는 불가능해, 보조 버튼의 밀도 트레이드오프로 의도적으로 수용(문서화). 실측(Maker Play, 스크린샷): 수정 전/후 비교로 버튼이 패널 경계 안에 완전히 들어옴 확인.
- ✅ **한탕주의 링크("한탕의 나눔": 파티 클리어 시 영구재화+10%) 신규 연결** — 2026-09-08. 불운의 "후원"과 완전히 동일한 `clearCurrencyBonus` 채널이라 신규 코드 없이 데이터만 추가(`linkEffect={clearCurrencyBonus=0.10}`, linkDesc 명시값 그대로). 빌드 로그 0 error, 실측(순수 함수, `GetLinkClearCurrencyBonus("한탕주의")==0.10` 확인). Phase C 진행률 67→68/115.
- ✅ **도굴꾼(hb) 5번째 무비용 익스플로잇 발견·수정** — 2026-09-08. hb(Hidden-bad) 전수 재점검 중 발견 — basicDesc "보스에게 받는 피해 2배, 대신 보스 처치 보상 3배"인데 실제 `basicEffect`는 `{exp=0.20}`(보스 무관 상시 +20% exp, "3배"와도 안 맞는 근사)뿐이었고 페널티("보스 피해 2배")는 프로젝트 어디에도 구현이 없어 — 침묵의맹세/고행자/시한부/운명론자에 이은 **5번째 무비용 익스플로잇**(이번엔 "(선행 필요)" 태그가 아예 없어 기존 태그 기반 감사로는 못 찾았던 사각지대). 양쪽 다 보스 전용으로 정확히 재구현: `Monster.GrantXpToAttacker`에서 `self.IsBoss`일 때만 처치 보상 ×3(상시 +20% 대신), `PlayerHit.HandleHitEvent`에서 공격자가 보스(`Monster.IsBoss`)일 때만 받는 피해 ×2 — 기존 mismatched `exp=0.20` `basicEffect`는 제거. 빌드 로그 0 error. 실 캐릭터 중 도굴꾼 보유가 없어(로스터 안전 지침) 보스 전투를 통한 라이브 검증은 코드 리뷰 수준으로 남음 — 로직은 기존에 여러 번 검증된 `HasTrait`/`IsBoss` 패턴을 그대로 재사용해 리스크 낮음.
- ✅ **미친 과학자(gmb) basicDesc 전면 미구현 발견·구현** — 2026-09-08. 링크 마스터를 찾은 것과 동일한 "basicEffect 필드 자체가 없는 항목" 재스캔에서 함께 발견(당시엔 UI 흐름 변경이 필요하다고 판단해 태그만 추가하고 보류) — 이후 재검토 결과 팝업 UI 자체를 바꾸지 않고도 구현 가능함을 확인해 구현 완료. **"선택 불가"**: 3택1 카드는 그대로 보여주되, `RequestResolveAbilityChoice`(서버) 최상단에서 클릭값(`choice`)을 무시하고 1~3 중 무작위로 덮어씀 — 운명론자의 "서버측 최종 방어선" 패턴과 동일한 원리(UI 변경 없이 서버가 결과를 통제). **"2배"**: 세 선택지("재분배"/"추가 스탯"/"특성 변경") 중 수치 배증 개념이 명확히 성립하는 "추가 스탯"에만 적용 — `ApplyBonusStats`에 `multiplier` 매개변수 추가(3포인트→6포인트), 재분배/특성변경은 배증 개념이 성립하지 않아 그대로 1배로 남김(정직한 스코프 제한, 문서화). 빌드 로그 0 error. **검증 범위**: `Str/Dex/Intel/Luk`은 로스터 영속 데이터라 실 캐릭터로 직접 실행하는 라이브 테스트는 로스터 안전 지침상 수행하지 않음 — 루프 카운트(`BonusStatPoints * multiplier`)와 무작위 선택 매핑(`roll 1/2/3 → redistribute/bonus/trait`) 둘 다 단순 산술이라 코드 리뷰 수준 검증으로 충분하다고 판단.
- ✅ **링크 마스터(hg) basicDesc 전면 미구현 발견·구현** — 2026-09-08. 지금까지의 QA는 전부 "basicEffect 필드는 있는데 소비처가 없는" 패턴만 찾아왔는데, 이번엔 반대 방향 — **basicEffect 필드 자체가 아예 없는** 경우를 발견. `TraitEffectEngine.GetLinkEfficiencyMultiplier`에 `트레잇=="링크 마스터"`일 때 1.2배를 반환하는 하드코딩이 있어 언뜻 구현된 것처럼 보였으나, 이건 "받는 링크 효과 전체 보정"이라는 완전히 다른 메커니즘(이름만 같음, 코드 옆 주석에도 근거 없음) — 실제 basicDesc("이능 획득 시 3초간 전 스탯 대폭 상승")는 프로젝트 어디에도 구현돼 있지 않았다. hg(Hidden-good, 7%) 풀 특성이 사실상 아무 기본 효과 없이 방치돼 있던 실질적 밸런스 결함. `RequestResolveAbilityChoice`(이능 선택지 해소 시점 = "이능 획득")에 트리거 삽입, "전 스탯"은 다른 임시버프와 동일하게 공격력 채널로 근사(+50%, 3초, 자체 근사치) — `LinkMasterBuffAtkPercent`/`LinkMasterBuffExpireElapsed` 신설, `BurstAtkPercent`(자석술사)와 완전히 동일한 만료 패턴으로 `CombatTick`에서 해제. 기존 1.2배 하드코딩은 그대로 유지(별개 효과로 판단, 임의로 제거하지 않음). 빌드 로그 0 error. 실측(Maker Play, **HideFromInspector 임시 전투 스탯을 직접 설정 후 원상복구 — 로스터 안전 지침이 명시한 "안전한 가역적 변형"에 해당, Trait/Level 등 영속 데이터 미접촉**): `LinkMasterBuffAtkPercent=0.50` 설정 시 `AttackDamage` 11.5→17.25(정확히 ×1.5) 확인 후 0으로 복원, 원래 값(11.5)으로 정확히 복귀 확인.
- ✅ **거리의 방랑자 링크("탐험 지원": 몬스터 게이트에서 스탯 구슬 획득량 대폭 증가) 신규 연결 + 고독/박애주의자 링크 텍스트 stale 태그 2건 추가 정정** — 2026-09-08. 링크 패시브 전체를 `linkName 있음+linkEffect 없음` 기준으로 재스캔(48건)해 발견. **거리의 방랑자**: 스탯 구슬을 경험치로 근사하는 기존 관행에 맞춰 "게이트 모드에서만" 적용되는 exp 보너스로 구현 — 상시 채널이 아니라 `Monster.GrantXpToAttacker`가 `FieldWaveSpawner.IsGateMode`를 직접 확인해야 해서 전용 게터(`GetLinkGateDropBonus`)+전용 `PlayerStatsComponent.LinkGateDropBonus`(@Sync, 링크효율 반영)로 분리, `linkEffect={gateDropBonus=0.30}`(자체 근사치, "대폭 증가"). **고독/박애주의자**: 재스캔 중 두 링크 텍스트에 "(현재 미구현 - 조건부 교차-슬롯 효과)"가 그대로 남아있는 걸 발견했으나, 실제로는 각각 `TraitEffectEngine.GetLinkEfficiencyMultiplierForSlot`(고독: 반대편 슬롯 링크효율×1.15)와 `PlayerStatsComponent.RecalculateDerived`의 전용 블록(박애주의자: 링크 2슬롯 모두 활성 시 전스탯+6% flat)으로 이미 완전히 구현돼 있어 — 순수 문서 stale, 텍스트만 정정. 빌드 로그 0 error. 실측(순수 함수 호출만, 필드 진입·전투 없음): `GetLinkGateDropBonus("거리의 방랑자")==0.30`, `GetLinkEfficiencyMultiplierForSlot("","","고독")==1.15`, `GetLinkEfficiencyMultiplier("박애주의자","")==3.0` 전부 확인. Phase C 진행률 66→67/115.
- ✅ **"(선행 필요)" stale 태그 3건 추가 정정** — 2026-09-08. 시한부/침묵의 맹세/고행자는 이번 세션 초반에 이미 페널티까지 실제로 구현·enforce된 상태였는데(각각 `PlayerStatsComponent.CombatTick` 180초 즉사 타이머, `PlayerSkillComponent.UseSkillSlot` 스킬 차단 가드, `PlayerStatsComponent.GrantXp` 스탯 자동증가 스킵 — 코드 확인함) `basicDesc` 문구만 "(선행 필요)"로 계속 남아있던 stale 표기. 봉인된자/외길/봉인구와 동일한 패턴(구현 vs 문서 표기 불일치)이라 3건 모두 텍스트만 정정(로직 변경 없음), 시한부는 겸사겸사 구체적 수치(180초)도 명시.
- ✅ **불운 링크("후원": 파티 클리어 시 영구재화+20%) 신규 연결** — 2026-09-08. 동일 패턴 3번째 — `linkDesc`에 이미 정확한 수치("+20%")가 있어 근사 없이 그대로 `linkEffect={clearCurrencyBonus=0.20}`로 데이터화. `PlayerStatsComponent.LinkClearCurrencyBonus`(@Sync, 링크효율 반영) 신설 + `GrantRunEndCurrency`의 본인 특성용 `bonus` 합산에 가산. 빌드 로그 0 error. 실측(순수 함수 호출만, 필드 진입·전투 없음): `GetLinkClearCurrencyBonus("불운") == 0.20` 확인.
- ✅ **도박꾼 링크("재화 폭죽": 처치 시 낮은 확률 재화 즉시 획득) 신규 연결** — 2026-09-08. 빈손의 "통행증"과 동일한 발견 패턴("선행 필요"였지만 필요한 서브시스템(계정 영구 재화, Phase G)이 이미 존재) — `PlayerStatsComponent`에 `LinkOnKillCurrencyChance`(@Sync, 링크 효율 계수까지 반영해 매 `RecalculateDerived`마다 재계산+클램프) 신설, `TraitEffectEngine.GetLinkOnKillCurrencyChance` 게터 추가, `linkEffect={onKillCurrencyChance=0.08}`(자체 근사치)로 데이터화, `Monster.GrantXpToAttacker`의 처치 판정 지점에 굴려서 성공 시 `AccountCurrencyLogic:GrantCurrency`로 재화+3 지급하도록 연결. 빌드 로그 0 error. 실측(Maker Play, **순수 함수 호출만 사용, 필드 진입·전투 트리거 없음**): `GetLinkModifiers("도박꾼").onKillCurrencyChance == 0.08`, 게터 반환값 일치, 무관한 특성에 대해서는 0 반환 확인. 실 캐릭터가 도박꾼을 링크로 보유한 경우가 없어 `Monster.GrantXpToAttacker`의 실제 처치-트리거 합산 판정 자체는 코드 리뷰 수준으로 남음.
- ✅ **빈손 링크("통행증": 게이트 입장권 획득 확률 증가) 신규 연결** — 2026-09-08. linkEffect 필드 자체가 없어 "선행 필요"로 미구현이던 것을, 게이트 시스템(Phase E)이 이미 존재함을 근거로 신규 연결 — `PlayerStatsComponent`에 `GateTicketChanceBonus`(@Sync, 매 `RecalculateDerived`마다 리셋 후 재누적) 신설, `TraitEffectEngine.ApplyModifiersToStats`에 범용 `gtc` 채널 추가, `linkEffect={gtc=0.05}`(+5%p, 다른 "낮은 확률"류 관행값과 동일선상 근사치)로 데이터화, `FieldWaveSpawner.RollGateTicketDrop`이 판정 시 유저별 `GateTicketChanceBonus`를 더해서 읽도록 수정. 빌드 로그 0 error. 실측(Maker Play, **순수 함수 호출만 사용, 필드 진입·전투 트리거 없음** — 직전 라이브 테스트 사고 재발 방지): `GetLinkModifiers("빈손").gtc == 0.05` 확인. 실 캐릭터 중 빈손을 링크로 보유한 경우가 없어(로스터 안전 지침) `RollGateTicketDrop`의 실제 합산 판정 자체는 코드 리뷰 수준으로 남음 — 로직은 `FieldWaveSpawner.mlua`의 단일 지점이라 리스크 낮음.
- ✅ **LUK 2차 스탯(EvasionPercent) 회피 파이프라인 신규 구현** — 2026-09-08. §8-1 원안대로 `RecalculateDerived`가 `EvasionPercent = effLuk*0.002`를 계산은 해왔으나 코드 주석에 "inert until a monster-attacks-player pipeline exists"라고 명시돼 있던 대로 실제로는 어디서도 참조되지 않았다(LUK 투자가 크리티컬 확률/데미지 외엔 사실상 의미 없었음) — 이 프로젝트 전체에서 가장 근본적인 QA 발견. `PlayerHit.HandleHitEvent`(몬스터 접촉 피해 진입점, 바로 그 "몬스터 공격 파이프라인")에 회피 굴림 추가 — 성공 시 피해/방어막소모/채널취소 등 아무 처리 없이 완전 무효화. 메아리의 링크("회피 성공 시 데미지 증가")도 이제 조건 자체는 실제로 성립 가능해졌으나, 파티원 회피 이벤트마다 링크 소스를 조회하는 배선은 범위가 커서 기존 처형자 등과 동일한 상시 flat 근사(`linkEffect={atk=0.02}`)로 유지. 실측(Maker Play): 실 플레이어 `EvasionPercent` 값이 `effLuk*0.002` 공식과 정확히 일치 확인 + **실제 `HitEvent`를 생성해(`totalDamage=0`으로 안전하게) 진짜 이벤트 파이프라인으로 발사**, `HandleHitEvent` 전체 경로(회피 굴림 포함)가 에러 없이 실행되고 Hp가 의도대로 불변임을 확인 — 이번 세션에서 실제 엔진 `HitEvent`를 직접 만들어 실전 이벤트 파이프라인으로 검증한 첫 사례(기존엔 전부 메서드 직접 호출 방식).
- ✅ **자석술사(처치 연쇄) 재점검 후 구현** — 2026-09-08. "드랍 흡수 서브시스템 선행 필요"로 막혀있었으나, 이 프로젝트의 드랍은 애초에 즉시 인벤토리 지급 방식(`Monster.TryDropPotion`→`GrantPotionDrop`, 걸어가서 줍는 필드 아이템 자체가 없음)이라 "흡수"할 대상이 없다 - 기본 효과("3초 내 3킬 시 5초간 공격력+30%")는 드랍 흡수와 무관한 별개의 킬스트릭 메커니즘이라 실제로는 막혀있지 않았음을 재확인. `PlayerStatsComponent.RegisterBurstKill`(윈도우 내 처치 카운트 + 발동) + `CombatTick`의 만료 체크로 구현, `Monster.GrantXpToAttacker`에서 처치마다 호출(Trait2도 커버). 실측(Maker Play, 실제 플레이어 컴포넌트에 직접 호출): 3킬 미만→버프 없음(0.0) → 3킬째→버프 발동(atk%=0.3, 만료 시각 정확히 now+5) → 만료 시각 경과 후 `CombatTick()`→버프 해제(0.0) 전체 사이클 정확히 일치 확인.
- ✅ **큐브마스터 이능 팝업 재추첨 연결** — 2026-09-08. 3장 카드 중 실제 "무작위 미리보기"를 갖는 카드는 특성 변경(trait) 하나뿐이라(재분배/추가 스탯은 결과를 미리 굴려 보여주지 않는 일반 설명 카드), 재추첨을 그 미리보기 하나를 다시 굴리는 것으로 구현. `ui/AbilityChoiceGroup.ui`에 `BtnReroll` 추가, `PlayerStatsComponent.RequestRerollAbilityPreview`(큐브마스터+팝업 열림 상태 서버 검증) → `AbilityChoiceUIController.UpdateTraitPreview`(팝업 안 닫고 텍스트만 갱신). 실측(Maker Play): 큐브마스터 케이스 버튼 노출+텍스트 정상 갱신, 일반/운명론자 케이스 버튼 자동 숨김, 실 플레이어(비-큐브마스터)로 서버 메서드 직접 호출 시 안전한 no-op 전부 확인.
- ✅ 5풀 확률 가중치(hb7%/gmb18%/hg20%/bad20%/avg35%) 구현 — `TraitRollLogic.mlua`. Hidden-bad 완화 천장(40회 무출현시 41회째 확정)도 구현했으나 **세션 한정 카운터**(월드 재시작 시 초기화, 계정 영구 저장은 미연결 — CharacterRosterLogic 저장 스키마 변경 리스크로 이번 패스 범위 밖). 실측: 200회 시도 중 10회 샘플로 5풀 전부 정상 출현 확인.
- ✅ 캐릭터 생성 시 특성 배정을 축소 풀(6종)에서 전체 115종 풀로 교체(`CharacterSelectStageController.InitRandomPools`/`CreateCharacter`) + 이능 "특성 변경"(`PlayerStatsComponent.RollDifferentTrait`)도 동일하게 전체 풀 사용하도록 교체.
- 🟡 3차 전직(13레벨) 추가 특성 1회 재추첨 연결 — **2026-09-06 구현 완료(범위 제한부)**. `PlayerStatsComponent.Trait2`(`@Sync`) 신설, `GrantSkillTierIfMilestone`의 tier==3 분기에서 `TraitRollLogic:RollDifferentTrait(Trait)`로 1회 자동 재추첨(부패한 계약서 보유 시 `RollHiddenPool()`로 Hidden 풀 강제). `TraitEffectEngine.ApplyToStats`를 `(stats, trait, ...)` 시그니처로 리팩터해 Trait/Trait2 각각 독립 호출로 17개 스탯 채널이 곱연산 합산되도록 구현, legacy 5특성(버서크/균형/절약가/단단함/날렵함) 체크 지점도 전부 `Trait == X or Trait2 == X`로 확장. 실측(2026-09-06): 강제 레벨업(GrantXp 50000)으로 Trait2="겁쟁이" 롤링 확인, 빌드 로그 0 error. **범위 제한(정직히 기록, 2026-09-08 갱신)**: 채널형 스탯(원래 갭)과 2단계 트리거 메커니즘(방어막/반사/스택/저체력/주기효과, 2026-09-08에 `CombatTick`/`PlayerHit`/`PlayerAttack`/`Monster`/`PlayerSkillComponent` 전체 15개 콜사이트를 "Trait 우선, 없으면 Trait2" 패턴으로 확장 완료 — 빌드 로그 0 error + 실 플레이어 `CombatTick()` 안전 호출로 런타임 에러 없음 확인)는 이제 Trait2를 반영한다. 여전히 미반영: 링크 패시브(애초에 Trait2는 로스터 미저장이라 링크 소스로 노출될 일이 없어 해당 없음). **CP 산식·스탯 탭은 사실 이미 반영돼 있었음(재확인)** — `attack.AttackDamage`/`CritDamageMultiplier`/`DamageReductionPercent` 등 라이브 컴포넌트 값을 읽으므로 Trait2의 채널형 효과가 `ApplyToStats` 경유로 이미 자동 반영됨(이전 기록이 부정확했음). **캐릭터 정보 UI의 "2차 특성" 표시만 진짜 갭이었음** — `.ui`(`CharacterInfoGroup.ui`)에는 `Trait2Title`/`Trait2Desc` 엔티티가 이미 있었으나 컨트롤러가 한 번도 바인딩/갱신하지 않아 항상 정적 "미해금" 문구만 보였다. `CharacterInfoUIController`에 두 프로퍼티 추가 + `RefreshInfoTab`에서 `statsComp.Trait2` 유무로 동적 표시하도록 연결, 실측(Maker Play, 실제 로스터 캐릭터로 `Refresh()` 호출) 확인. 로스터 영구 저장도 안 함(레벨 자체가 이미 로스터 미저장인 기존 한계와 동일 특성 — 재입장 시 소실, 신규 회귀 아님).
- 🟡 기본 패시브 UI 텍스트 115종 전체 연결 완료(`AbilityChoiceUIController.TraitBlurb`/`CharacterInfoUIController.TraitDesc`가 legacy 6종 제외 전부 `TraitCatalog.GetTraitDef` 조회로 표시) — 수치 자체는 위 엔진 커버리지에 따름.
- 🟡 링크 패시브 115종 실제 효과 적용 — Phase C 범위, 2026-09-08 기준 74/115종 진행(빈손/도박꾼/불운/거리의 방랑자/한탕주의/버서크/회전룰렛/금욕/동전 던지기 사형집행인/유리몸/순교자 링크 신규 연결 포함, 아래 Phase C 상세 참고).
- 🟡 실측 밸런스 검증 — 115종 전체 결정론적 시뮬레이션(버서크 재설계 때 방식)은 여전히 미착수(대부분 미구현 효과라 시뮬레이션 자체가 무의미). 대신 2026-09-08에 코드 레벨 QA 스윕으로 `basicEffect`/게터 전수 조사 → 무비용 익스플로잇 4건 발견·수정 + 게터 없이 방치된 갭 5건(럭키세븐/자석술사/신중함/천부적재능/스피드스터) 발견·구현 완료(아래 Phase B/J 상세 참고) — 다른 방식의 밸런스 QA로 실질 대체.

### Phase C — 링크 캐릭터 실효과 연결
- ✅ **버그 수정(선행)**: 로스터의 캐릭터별 `level` 필드가 생성 시점(1) 이후 전혀 갱신되지 않던 기존 결함 발견·수정 — `PlayerStatsComponent.OnSyncProperty("Level")`이 레벨업마다 `CharacterRosterLogic:UpdateActiveLevel()`을 호출해 즉시 저장. 이 결함 때문에 "링크 캐릭터 15레벨 이상" 조건이 지금까지 실질적으로 영원히 충족 불가능했음(모든 링크 후보가 항상 레벨 1로 보임) — 2026-09-06 실측: 테스트 캐릭터 레벨 1→4 강제 후 로스터 조회로 즉시 반영 확인.
- ✅ 링크 패시브 115종 중 63종에 수치 효과(`linkEffect`) 입력 완료(57종 → 2026-09-08 세션에서 6종 추가: 연성술사/양날검/폭탄 돌리기/오염의 씨앗/외길/외눈 — 각각 기존 basicEffect 수치의 1/3~1/4을 flat 링크 채널로 근사, `TraitEffectEngine.GetLinkModifiers`로 실측 반환값 확인). 나머지 52종은 여전히 새 서브시스템 필요(랜덤 버프 디스패치/DOT 프레임워크/스냅샷 롤백/누적 판수 성장/조건부 교차-슬롯 등) — 정직하게 `linkEffect` 필드 미기입 상태로 유지. **추가 채택 기준(정직히 기록)**: linkDesc 텍스트가 순수 UI/정보성 효과(예: 상인의 "구슬 종류 미리 표시")이거나 이 세션의 다른 채널(게이트 입장권 드랍률 등 미구현 선행 시스템)을 가리키는 경우는 의도적으로 제외 — flat 스탯 근사가 텍스트와 명백히 어긋나는 경우까지 억지로 수치화하지 않음. — `TraitEffectEngine.ApplyLinkToStats`/`ApplyModifiersToStats`(본인 특성용 `ApplyToStats`와 공유)로 구현.
- ✅ 15레벨 이상 링크 캐릭터의 링크 패시브를 실제로 스쿼드(액티브 캐릭터)에 적용 — `PlayerStatsComponent.UpdateSquadLinks`(Server RPC, senderUserId 검증) + `CharacterRosterLogic.PushSquadLinksToServer()`(공격대 확정 직후·캐릭터 진입/교체 직후 호출). 동일 링크 패시브(같은 특성) 중복 불가 규칙 구현(2번째 링크가 1번째와 같은 특성이면 무시).
- ✅ 링크 마스터(1.2배)/박애주의자(3배)/친화력(1.1배)/외골수(0.5배)/고독(0배, 링크 전부 차단) 효율 보정 — `TraitEffectEngine.GetLinkEfficiencyMultiplier(trait, trait2)`. 2026-09-06 실측: 외골수 적용 시 처형자(atk+3%) 링크 효과가 정확히 0.5배(attackDamage 17.5→17.7625, 이론값과 소수점까지 일치)로 스케일링됨을 확인.
- ✅ 공격대 편성 UI에 링크 패시브 미리보기 추가 — 신규 UI 노드 대신 `SquadFormationController`의 기존 후보 라벨 텍스트에 링크 패시브 설명을 추가하는 방식으로 구현(빠른 구현, 별도 `.ui` 빌더 작업 불필요). 15레벨 미만이면 잠긴 패시브 이름만, 15레벨 이상이면 실제 효과 설명까지 표시.
- ✅ 고독/박애주의자의 "다른 슬롯 링크효율 보정" 교차-슬롯 효과 — 2026-09-08 구현+실측 검증 완료. `TraitEffectEngine.GetLinkEfficiencyMultiplierForSlot(trait, trait2, otherSlotTrait)` 신설(반대편 링크 슬롯이 고독이면 ×1.15) + `RecalculateDerived`가 기존 단일 `linkEfficiency` 공유 대신 슬롯별로 `linkEfficiency1`/`linkEfficiency2`를 따로 계산하도록 교체. 박애주의자는 "두 슬롯 모두 활성 시 전스탯+6%" 플랫 보너스로 별도 구현(본인이 고독이라 링크를 아예 못 받는 상태면 이 보너스도 함께 차단). 실측: `GetLinkEfficiencyMultiplierForSlot`을 4가지 조합(고독 파트너 유무·본인 링크마스터 중첩·본인이 고독인 경우)으로 호출해 1.15/1.0/1.38/0 전부 설계값과 정확히 일치 확인.
- 🟡 **범위 제한(정직히 기록)**: (1) 링크 캐릭터 본인의 `Trait2`(3차 전직 추가 특성)는 애초에 로스터에 저장되지 않아(레벨과 동일한 기존 한계) 링크 소스로 반영 불가 — 로스터가 캐릭터당 특성 1개만 기록하는 구조적 제약. (2) 결정론적 밸런스 시뮬레이션(링크 조합 단위)은 미실행. (3) 박애주의자 플랫 보너스의 실 플레이 적용(`RecalculateDerived` 내부 분기 자체)은 로스터 안전 지침상 실 캐릭터로 라이브 검증하지 못함 — 순수 함수 부분만 실측, 나머지는 코드 리뷰 수준.

### Phase D — 전직 시스템 완성 (4차, 메이플 옛날 직업 스킬 하이브리드)
- ✅ 기존 `SkillCatalog.mlua`(13종, 1차 한정)를 4차 전직 전체로 확장 — 5직업 x 3티어(2/3/4차) x 버프/공격 = 신규 30종 추가(총 43종). `tier` 필드 신설 + `GetSkillsByCategory`/`RollSkillChoices`가 tier 파라미터로 필터링하도록 리팩터(이전엔 티어 구분 없이 카테고리 전체에서 뽑아 5레벨 캐릭터가 4차 스킬을 받을 수 있는 구조적 결함이 있었음). 스킬명은 메이플스토리 지식 기반 최선 추정(더블스윙/파워가드/썬더볼트/트리플스로우/아수라/인피니티/허리케인/메테오스트라이크/배틀쉽 등 다수는 높은 확신으로 실존 확인, WebSearch 교차검증 시도했으나 구버전 위키 자료 부족으로 전부 백과사전 수준 검증은 아님 — 기존 럭키세븐/너클부스터와 동급의 타협). VFX는 시간 관계상 각 직업 기존 1차 스킬 RUID 재사용(신규 msw-search 없음, 향후 폴리싱 과제로 명시). 신규 testableEffect 3종(dmgreduction/skilldmg/atkbonus)을 `PlayerSkillComponent.DoBuff/RevertBuff`에 ironbody와 동일한 델타 저장 패턴으로 구현. **실측 검증(2026-09-06)**: `GetSkillsByCategory`로 티어별 정확한 개수(1차 7atk/6buff, 2~4차 각 5atk/5buff, 총 43종) 확인, 실제 레벨5 캐릭터의 3택1 팝업이 2차 풀(meditation/powerguard/soularrow)만 보여주고 다른 티어가 섞이지 않음을 확인, 버프→공격 순차 진행 확인, dmgreduction(0.0→0.15)/skilldmg(0.15→0.35)/atkbonus(100→120) 3종 신규 효과 전부 정확한 수치로 적용됨을 clean 격리 테스트로 확인. 빌드 로그 에러 0.
- ✅ 전직 퀘스트 보스 연결 (5·13·18레벨, 클리어해야 다음 차수 스킬 획득) — `PlayerStatsComponent`에 `PromotionPending2~4`(@Sync) 신설. 레벨 요건 충족 시 곧바로 `GrantTierSkills`를 호출하던 기존 동작을 제거하고 대신 대기 플래그만 세운다(`GrantSkillTierIfMilestone`). 실제 지급은 `RecordBossKillForPromotion`(신규)에서 수행 — `Monster.GrantXpToAttacker`에 `if self.IsBoss then stats:RecordBossKillForPromotion() end` 3줄 추가로 연결(기존 `FieldMiniBoss`/`AddKill` 인프라 재사용, 전용 퀘스트 맵 신규 제작 없음). 여러 티어가 동시에 대기 중이면 가장 낮은 티어부터 하나씩 해소. **실측 검증(2026-09-06)**: 레벨5/13 강제 도달 시 스킬 팝업 없이 "처치 대기" 로그만 남는 것 확인 → `RecordBossKillForPromotion` 직접 호출(보스 처치 시뮬레이션)로 tier2 스킬 3택1 팝업이 그제서야 발동하는 것 확인, tier3도 동일하게 검증 + Trait2(3차 추가 특성)도 이 시점에야 굴러가는 것 확인. 빌드 로그 에러 0.
- ✅ 보스 클리어 타임 계정 귀속 기록 (리더보드 연동, Phase H) — 범위 제한부. `PromotionMilestoneElapsed`(레벨 달성 시각) + `LastPromotionClearSeconds`(처치까지 걸린 시간, 세션 내 최근 값)를 `RecordBossKillForPromotion`이 계산·로그로 기록. **미구현**: 계정 귀속 영구 저장(`_DataStorageService`)과 전역 리더보드 자체는 Phase H 몫으로 명시적으로 남김 — 지금은 세션 로그로만 확인 가능.
- ✅ 봉인된 자(1차 한정)/외길(2슬롯 한정)/봉인구(4차 불가) 등 특성-전직 상호작용 규칙 연결 — `HasTrait(name)`(Trait/Trait2 겸용 조회) 신설. 봉인된 자: `GrantSkillTierIfMilestone`에서 tier 무관 전부 무시(전직 자체가 안 세워짐). 봉인구: tier==4만 차단. 외길: 보스는 정상 처치되고 Trait2/레벨 진행은 막지 않되 `RecordBossKillForPromotion`이 `GrantTierSkills` 호출만 스킵. **실측 검증**: 3개 특성 전부 개별 격리 테스트로 의도한 분기(봉인된자→tier2_pending 안 세워짐, 봉인구→tier2는 세워지고 tier4만 차단, 외길→처치는 확인되지만 스킬 미지급) 확인.
- ⚠️ **테스트 중 발견한 기존 버그(이번 패스 범위 밖, 정직히 기록)**: `CharacterRosterLogic.UpdateActiveLevel`이 `PlayerStatsComponent.Level` 변경 시 그 컴포넌트가 실제로 "현재 활성 캐릭터"인지 검증 없이 무조건 `GetActiveIndex()` 슬롯에 레벨을 덮어쓴다. 정상 플레이 흐름(슬롯 선택→입장)에서는 문제가 없지만, 이번 테스트처럼 choice_map에 파킹된 로컬 플레이어 엔티티의 스탯을 직접 조작(정식 입장 플로우 우회)하면 실제 활성 슬롯(테스트 중엔 슬롯1, 실유저 캐릭터)의 level 필드가 의도치 않게 덮어써질 수 있음 — 실제로 테스트 중 슬롯1의 level이 1→13으로 잘못 저장되는 것을 발견, 즉시 `roster[1].level=1` 복구 후 재시작으로 영구 저장 확인(trait/스탯/스킬 등 다른 필드는 전혀 손상 없었음, 재확인 완료). 다음 세션 참고: 파킹 플레이어로 스탯 테스트 시 이 사이드이펙트를 염두에 두거나, 테스트 후 항상 슬롯1 level을 재확인할 것.

### Phase E — 몬스터 게이트 (특수 던전)
- ✅ 게이트 입장권 재화 + 인벤토리 연결 — 2026-09-07 구현+실측 검증 완료. 신규 `Meta/GateTicketLogic.mlua`(`@Logic`, AccountCurrencyLogic과 동일한 계정 UserDataStorage 패턴, 최대 10장). 일반 전장 클리어(미니보스 처치) 시 20% 확률로 1장 드랍(`FieldWaveSpawner.RollGateTicketDrop`). `ui/BattlefieldSelectGroup.ui`에 신규 Field4 행("몬스터 게이트" + 보유 수량 표시) 추가, `BattlefieldSelectController.OnGateClicked`→`GateTicketLogic.RequestEnterGate`로 연결. 실측: `GrantTicket`→`LoadCount`→클라이언트 `OnTicketGranted` 왕복 전부 로그로 확인(count 0→1).
- ✅ 좌우 조여오는 몬스터 웨이브 패턴 — 전용 맵을 새로 만들지 않고 **토벌전장3(기존 MapleTile 맵)을 게이트 모드로 재사용**(`FieldWaveSpawner.IsGateMode` 플래그, `GateTicketLogic.RequestEnterGate`가 진입 직전 세팅, `ResetField`가 방문자 교체마다 해제 — 솔로 전용 게임 전제하 저위험 설계, 새 맵 제작에 필요한 유저의 TileMapMode 수동 설정을 회피). 웨이브가 진행될수록 좌우 스폰 X가 중앙으로 수렴(`GateConvergePerWave`/`GateMaxConverge`).
- ✅ 스탯 3배/물량 2배 스케일링 — `GateStatMultiplier=3.0`/`GateAliveMultiplier=2.0`을 `TrySpawn`/`SpawnMiniBoss`에 적용. **실측(2026-09-07, 실제 몬스터 스폰)**: 전장3 기본 FieldMonster3(MaxHp 280/ATK 32/XP 120) → 게이트 모드에서 MaxHp=840.0/ATK=96.0/XP=360 정확히 3배 확인(로그 증거). 동시 생존 상한도 2배 적용.
- 🟡 게이트 보스 등장 + 클리어 보상 — 구현 완료, **보스/보상 부분은 코드 리뷰로만 검증**(실측 미완, 아래 참고). 기존 `SpawnMiniBoss`(마지막 웨이브 트리거)를 그대로 재사용해 `GetStatMultiplier()`/`GetRewardMultiplier()`로 보스 스탯도 스케일링(전장3 보스 기준 HP 400→1200/ATK→3배/XP→3배 예상, TrySpawn과 동일 검증된 배율 로직 재사용이라 리스크 낮음). 클리어 시 `GrantGateClearRewards()`(이능 재선택권=`PendingAbilityChoices+1`, 고급 스탯 구슬=Str/Dex/Intel/Luk 각 +3 — 둘 다 이미 검증된 기존 메서드/필드 재사용)를 호출. **실측 미완 이유**: 라이브 테스트 중 실제 사용자 캐릭터(슬롯1)가 필드 몬스터에게 피격사망하는 사고가 발생해(로스터 데이터 자체는 무손상 확인 완료) 추가 라이브 조작을 자제하고 코드 리뷰로 전환 — 다음 세션에서 별도 테스트 캐릭터로 보스 처치까지 실측 권장.

### Phase F — 난이도 4단계 스케일
- ✅ 하급/중급/상급/최상급 몬스터 스탯 배율(×1.0/1.5/2.25/3.4) + 보상 배율(×1.0/1.4/2.0/2.8) 적용 — `FieldWaveSpawner.DifficultyTier`(1~4) + `GetStatMultiplier()`/`GetRewardMultiplier()`. 게이트 모드와는 상호 배타적으로 처리(게이트가 우선). 2026-09-07 실측: tier 1~4 각각 정확히 1.0/1.5/2.25/3.4(스탯)·1.0/1.4/2.0/2.8(보상) 반환 확인.
- ✅ 토벌전장 선택 UI에 난이도 선택 추가 — 2026-09-08 구현+실측 검증 완료. `ui/BattlefieldSelectGroup.ui`에 4개 토글 버튼(하급/중급/상급/최상급) 추가, `BattlefieldSelectController`가 `selectedTier`(기본 1)를 유지하며 `OnField1/2/3Clicked`에서 `_CharacterRosterLogic:RequestEnterBattlefield(fieldIndex, selectedTier)`로 전달. `RequestEnterBattlefield`는 목적지 맵의 `FieldWaveSpawner`를 `_EntityService:GetEntityByPath`로 찾아 텔레포트 직전에 `DifficultyTier`를 세팅. 실측(Maker Play, client+server_main 양쪽): (1) client에서 4개 tier 버튼 UUID 바인딩 정상, `selectedTier` 갱신 및 `RefreshTierButtons()` 정상 동작 확인 (2) server_main에서 `/maps/토벌전장1` → `FieldWaveSpawner` 조회 후 `DifficultyTier=3` 세팅 시 `GetStatMultiplier()=2.25`/`GetRewardMultiplier()=2.0` 정확히 반환 확인(배율표와 일치). 실 유저 캐릭터로 텔레포트해 몬스터 스탯까지 확인하는 전체 E2E는 이번 세션의 로스터 안전 지침(슬롯1 보호)상 보류 — 컴포넌트 레벨 로직은 검증 완료.

### Phase G — 재화 · 계정 메타 진행
- ✅ 영구 재화 시스템 (런 클리어 정산 → 계정 귀속) — 2026-09-06 구현+실측 검증 완료. 신규 `Meta/AccountCurrencyLogic.mlua`(`@Logic`, RestGaugeLogic과 동일한 계정 UserDataStorage 패턴). `PlayerStatsComponent.GrantRunEndCurrency()`가 `TriggerRunEnd()`(사망/ENDING) 시점에 `도달레벨×5 + 처치수×2 + floor(생존초/10)` 공식으로 정산·적립. Maker 실측: level=7/kills=12/survival=95s → base=68, 정확히 일치 확인, 계정에 68 적립 후 클라이언트 `OnCurrencyGranted` 왕복까지 로그로 확인.
- ✅ 상인 특성의 실제 드랍률 효과 — 이미 M2 Phase B(TraitCatalog `basicEffect={drop=0.15}` + `Monster.TryDropPotion`의 `DropRateMultiplier`)에서 완료된 상태였음을 재확인(25차 처치경험치 임시 대체는 이미 원복됨). 이번 세션은 검증만 수행.
- ✅ 계정 영구 강화 — 2026-09-06 구현+실측 검증 완료(설계 범위: "시작 스탯 보너스" + "좋은 특성 가중치 상승" 통합 1개 트랙, 레벨 0~10, 비용 50×(레벨+1) 선형 누진). `CharacterSelectStageController.RollStats()`가 레벨당 +1 총 스탯 포인트(실측: 레벨1→합계21 확인), `TraitRollLogic.RollPool(bonusLevel)`이 레벨당 hb/bad에서 0.5%p씩 걷어 hg로 이동(레벨10 상한 5%p, 실측: 레벨1/60롤 표본에서 에러 없이 정상 동작 확인 — "캐릭터 생성 시"로 범위 한정, 이능 재추첨 등 런 중간 재추첨은 미적용). 신규 `Meta/AccountUpgradeUIController.mlua` + `ui/AccountUpgradeGroup.ui`(파치먼트/브라운 톤, 기존 EndingGroup 패턴 재사용) + choice_map에 "강화" 월드 버튼(`WorldButton.model` 신규 인스턴스) 배치. 실측: 팝업 오픈→구매(정수 436→436-100 남았어야 할 다음 비용까지 텍스트 정확 표시)→레벨 1 반영까지 로그+엔티티 텍스트 직접 조회로 확인. **게이트 입장권 보유 한도 증가는 미구현**(몬스터 게이트 자체가 Phase E 미착수라 입장권 시스템이 없음 — 정직하게 기록).
- ✅ 빈곤의 서약·한탕주의 특성의 특수 재화 규칙 — 2026-09-06 구현+실측 검증 완료. `GrantRunEndCurrency`가 두 특성을 트레잇 이름으로 직접 분기(기존 봉인된 자/외길/봉인구 패턴과 동일 원칙): 빈곤의 서약=항상 0(실측 확인), 한탕주의=평소 0(실측 확인)·클리어 시에만 기본의 5배(실측: base68→340 정확히 일치). 시간여행자(`clearCurrencyBonus=0.15`)도 함께 구현+실측(68→78, floor(68×1.15) 일치).

### Phase H — 리더보드 · 명예의 전당
- ✅ `msw-packages`의 `ranking-basic-package` 확인 완료 — World-shared `SortableDataStorage` 기반이라 컨셉은 맞았으나, 이 프로젝트는 솔로 전용 게임이고 패키지는 admin 권한 체크·`WorldConfig.PlayerEntityAuthorityCheck` 활성화·다중 Logic 파일(RankingDataStorageLogic/RankingDataCacheLogic/AdminLogic 등) 등 MMO 랭킹 UI 전제의 부가 설정이 많아, 이번 프로젝트 규모 대비 오버스펙으로 판단 — 패키지 미채택, `_DataStorageService:GetSortableDataStorage`를 직접 쓰는 자체 구현으로 대체(아래).
- ✅ 전직 퀘스트 보스 클리어 타임(4차/최종만, 헤드라인 지표) 전역 리더보드 — 2026-09-08 구현+실측 검증 완료. 신규 `Meta/LeaderboardLogic.mlua`(`@Logic`) — `_DataStorageService:GetSortableDataStorage("LB_PromotionClear4")`에 `DataStorageKeyInfo(profileCode, "", nickname)`로 닉네임을 Tag에 실어 저장(값=클리어초×10, 정수 전용 제약 우회), 기존 기록보다 느리면 Set 자체를 스킵(Get 1회 비교). `PlayerStatsComponent.RecordBossKillForPromotion`이 tier==4 처치 시 `_LeaderboardLogic:SubmitPromotionClearSeconds` 호출하도록 연결.
- ✅ ENDING 화면 → 명예의 전당 등록 연결(기존 개인 최고 기록을 전역으로 확장) — `BestRecordLogic.RequestSubmitRun`이 `isNewLevel`(개인 최고 레벨 갱신) 판정 시 `_LeaderboardLogic:SubmitBestLevel`을 함께 호출해 `LB_BestLevel` 전역 보드에 반영. `EndingGroup.ui`에 "명예의 전당" 버튼 추가 → 신규 `ui/LeaderboardGroup.ui`(레벨/클리어타임 top10 2열) + `Field/LeaderboardUIController.mlua`(`@Logic`)로 조회 결과 표시. **실측(Maker Play, server_main+client 양쪽)**: 테스트 3계정(TEST_A/B/C) 레벨 12/21/7 + 클리어타임 145.7/88.2/200.0초 제출 → `RequestTopBestLevel`(내림차순)/`RequestTopPromotionClear`(오름차순) 조회 결과가 UI 텍스트에 `1. 테스터B Lv.21 / 2. 테스터A Lv.12 / 3. 테스터C Lv.7`, `1. 테스터B 88.2초 / 2. 테스터A 145.7초 / 3. 테스터C 200.0초`로 정확히 정렬 반영됨을 확인, 테스트 데이터는 검증 직후 `DeleteAndWait`로 정리. 실 유저 계정으로 개인 최고 레벨 갱신 → 자동 전역 반영까지의 E2E는 로스터 안전 지침상 보류(로직 단위는 검증 완료).

### Phase I — 레벨 21 + 경험치 테이블 재조정
- ✅ §8-3 1~21레벨 경험치 테이블 도입(기존 MVP 선형 램프 `100+(레벨-1)*50` 제거) — 2026-09-08 구현+실측 검증 완료. `PlayerStatsComponent.ComputeXpToNextLevel`을 원안 표 기반 21종 테이블로 교체. **재산정 방식(정직히 기록)**: 원안이 명시한 "구 타깃 10~20분 → 신규 타깃 1시간" 배율(중간값 15분 기준 ×4)로 전체 수치를 균일 스케일링해 1~14 완만한 증가 / 15~21 급증(레벨당 1.6~1.9배) 곡선 형태는 그대로 유지. **이 ×4 배율 자체는 미검증**(1시간짜리 실측 런을 이번 세션에서 돌릴 방법이 없음) — 원안 §8 서두의 "베이스라인, 플레이테스트 후 조정"과 동일한 성격의 근사치이므로 실제 사용자 플레이 템포를 보고 후속 조정 권장. 실측: Maker Play에서 `ComputeXpToNextLevel(1~20)` 반환값이 설계한 테이블(4000/720/1200/.../752000)과 정확히 일치 확인.
- ✅ 최대 레벨 5~10(MVP, 사실상 무제한 - 기존 코드에 상한 자체가 없었음) → 21로 확장 + 16~21레벨 해금 조건(계정 귀속) 설계·연결 — 신규 `PlayerStatsComponent.UnlockedMaxLevel`(`@Sync`, 기본 15) + `ComputeUnlockedMaxLevel(upgradeLevel)`. **설계 결정(정직히 기록)**: 원안 부록 '미결 항목'에 "16~21레벨 해금 조건 구체값 - 각 레벨별 조건 별도 설계 예정"으로 미정으로 남아있던 항목을, 새 시스템을 만드는 대신 이미 존재하는 계정 영구 강화 트랙(M2 Phase G `AccountCurrencyLogic`, 0~10레벨)에 연결해 확정 — 강화 0~4레벨은 캡 15 고정, 강화 5~10레벨이 순서대로 16~21(만렙) 해금. `GrantXp`가 `self.Level < self.UnlockedMaxLevel` 조건으로 레벨업 루프를 멈추고 캡 도달 시 XP 축적 자체를 중단. `CharacterCardController.OnPlayClicked`이 매 입장마다 최신 계정 강화 레벨을 `RequestEnterGame`의 `stats.upgradeLevel`로 전달(캐릭터 생성 시점이 아닌 입장 시점 값이라 계정을 나중에 강화해도 기존 캐릭터가 즉시 혜택받음) → `ApplyStats`가 `ComputeUnlockedMaxLevel` 호출. 실측: Maker Play에서 `ComputeUnlockedMaxLevel(0~10)` 반환값이 설계값(15/15/15/15/15/16/17/18/19/20/21)과 정확히 일치 확인. 🟡 **범위 제한**: `GrantXp`의 레벨업 중단 로직 자체(캡 도달 시 실제로 레벨업이 멈추는지)는 실 캐릭터(슬롯1) 안전 지침상 라이브로 발화시켜 검증하지 못함 — 코드 리뷰 수준으로만 확인, 다음 세션에서 테스트 캐릭터로 검증 권장.

### Phase J — 최종 통합 QA
- ⬜ 전 콘텐츠 실플레이 재검증 (신규 캐릭터 생성 → 전직 4회 → 몬스터 게이트 → 사망/ENDING) — 로스터 안전 지침상 이번 세션 보류.
- 🟡 밸런스 실측 — 115종 전체 결정론적 시뮬레이션은 미착수(원안 §부록 "밸런스 점검 1순위"의 처형자/흡혈귀/저격수/한탕주의/미친 과학자와 패시브/링크 우선순위 항목 대부분이 아직 서브시스템 미구현이라 수치 시뮬레이션 자체가 무의미 — CP 산식이 캡처하지 않는 흡혈/조건부 효과가 많음). 대신 **2026-09-08 코드 레벨 밸런스 점검**으로 실제 익스플로잇 3건을 발견·수정: `basicDesc`에 페널티가 "(선행 필요)"로 명시된 Hidden-bad 특성 중, 보너스(`basicEffect`)는 실제로 적용되면서 페널티는 강제되지 않아 **사실상 무비용 순수 버프**가 되어 있던 경우를 전수 확인했다.
  - **침묵의 맹세**: "MP 소모 스킬 사용 불가" 미강제 + 평타+100% 상시 적용 → `PlayerSkillComponent.UseSkillSlot`에 트레잇 체크 추가해 스킬 사용 자체를 차단.
  - **고행자**: "레벨업 시 스탯 자동 증가 없음" 미강제 + 경험치+50% 상시 적용 → `PlayerStatsComponent.GrantXp`의 레벨업 루프에서 STR/DEX/INT/LUK 자동 증가를 스킵하도록 수정.
  - **시한부**: "제한시간 초과 시 즉사" 미강제 + 경험치·드랍+40%/+40% 상시 적용(사실상 리스크 없는 순수 버프) → `CombatTick`에 180초(원안에 정확한 수치 없어 자체 설정, 미검증 근사치) 제한시간 타이머 추가, 초과 시 `player:ProcessDead()` + `TriggerRunEnd()`.
  - 세 건 다 빌드 로그 0 error + Maker Play에서 `CombatTick()`/`UseSkillSlot()` 안전 호출(non-시한부/non-침묵의맹세 캐릭터 기준, 분기 자체가 no-op임을 확인)로 런타임 에러 없음 확인. **실제 해당 특성을 보유한 캐릭터로 분기 진입 자체를 라이브 검증하지는 못함**(로스터 안전 지침 + 해당 특성 보유 캐릭터가 현재 없음) — 코드 리뷰 수준.
  - **운명론자** (2026-09-08 추가 발견): "이능 재추첨·특성 변경 불가" 미강제 + 시작 스탯+40% 상시 적용 → `RequestResolveAbilityChoice`의 `choice=="trait"` 분기에 트레잇 체크 추가(선택 소모 안 하고 무시, 다른 두 선택지는 정상 동작 유지) + `AbilityChoiceUIController.ShowChoice`가 클라이언트에서도 "특성 변경" 버튼을 미리 숨김(죽은 버튼 노출 방지). "이능 재추첨"(큐브마스터의 `rerollChoice`) 자체는 애초에 아무 특성도 못 쓰는 미구현 상태라 해당 특성만의 갭이 아님 — 손댈 필요 없음. 빌드 로그 0 error, 실 캐릭터로 라이브 검증은 보류(코드 리뷰 수준, 다른 3건과 동일 사유).
  - 나머지 hb/hg/gmb 특성들도 "(선행 필요)" 페널티가 있는 경우 전수 재확인함(외길/봉인구/봉인된 자는 이미 Phase D에서 다른 방식으로 정상 강제되고 있음을 재확인 — 문제 없음). 남은 "(선행 필요)" 태그는 전부 진짜 새 서브시스템이 필요한 것들(도트/스냅샷/소환/재화 등)뿐임을 확인 완료 — 무비용 익스플로잇 전수 조사 마무리.
- ⬜ `Archive/As-built.md` 최종화 + M2 마일스톤 클로즈

## 3b. 발견된 버그 (2026-09-07, Phase E/F 세션 중, 미수정)
- ✅ **`CharacterSelectStageController.CreateCharacter(idx)` "행" 이슈 — 2026-09-08 정황 증거로 해소 처리**: 세션 중 로스터를 관찰한 결과 슬롯 1~6 전부(레벨1, 서로 다른 특성)가 정상적으로 채워져 있음을 확인 — 이번 세션은 `CreateCharacter`를 단 한 번도 직접 호출하지 않았으므로(로스터 안전 지침 준수) 이 6개 슬롯은 실제 유저의 정상 클릭 플로우로 생성된 것으로 보이며, 클릭 경로 자체는 정상 작동함을 강하게 시사한다. 이전에 관측된 "멈춤"은 `maker_execute_script`로 `OnBeginPlay` 외부에서 직접 호출할 때만 재현되던 콘솔 호출 특유의 현상으로 결론 — 실 플레이에 영향 없는 것으로 판단, 추가 조사 불필요.

## 4. 실행 원칙
- 각 Phase는 하위 항목 완료 즉시 `⬜→🟡→✅`로 갱신 (msw-planning 규칙)
- 매 항목 구현 후 Maker 실측 검증 필수, 빌드 로그 에러 0 확인
- 완료 단위마다 커밋 + 푸시 (유저 승인 불필요 — 유저 지시로 자율 진행 중)
- 기존 검증된 M1 시스템(콜리전 그룹, i-frame, 웨이브 스폰 등)의 기존 동작을 깨지 않도록 회귀 테스트 병행
